<!--<p style="color: green"><%#= notice %></p>

<%#= render @document %>

<div>
  <%#= link_to "Edit this document", edit_document_path(@document) %> |
  <%#= link_to "Back to documents", documents_path %>

  <%#= button_to "Destroy this document", @document, method: :delete %>
</div>-->

<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <div class="card shadow-lg border-0 rounded-4">
        <div class="card-body px-5 py-4">
          <h2 class="text-center mb-4 fw-bold text-primary">Document Details</h2>

          <div class="mb-3">
            <div class="d-flex justify-content-between align-items-center border-bottom pb-2">
              <strong>Employee Name:</strong>
              <span><%= @document.employee.name %></span>
            </div>
          </div>

          <div class="mb-3">
            <div class="d-flex justify-content-between align-items-center border-bottom pb-2">
              <strong>Document Name:</strong>
              <span><%= @document.name %></span>
            </div>
          </div>

          <div class="mb-4">
            <div class="d-flex justify-content-between align-items-center border-bottom pb-2">
              <strong>Document Type:</strong>
              <span><%= @document.doc_type %></span>
            </div>
          </div>

          <% if @document.image.attached? %>
            <div class="text-center mt-5 mb-4">
              <%= image_tag @document.image, class: "img-fluid rounded shadow-sm", alt: "Document Image" %>
            </div>
          <% end %>

          <div class="text-center">
            <%= link_to "Back to List", documents_path, class: "btn btn-outline-secondary me-2 px-4" %>
            <%= link_to "Edit", edit_document_path(@document), class: "btn btn-primary px-4" %>
            <%= button_to "Delete", @document, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger mt-2 ms-2 px-4" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
